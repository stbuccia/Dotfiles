if (has("termguicolors"))
 set termguicolors
endif
"color theme (available only for neovim)
"colorscheme OceanicNext
if !has('nvim')
    colorscheme nord
endif
""color theme (available only for neovim)
syntax enable
